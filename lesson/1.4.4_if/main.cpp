#include <iostream>
/*
 * 主函数
 * 读取三个值, 计算结果
 */
int main() {
    int v1 = 0, v2 = 0, val = 0;
    std::cout << "请输入整数并以空格分开:" << std::endl;
    std::cin >> v1 >> v2;
    if (v1 < v2) {
        
        std::cout << cur << " 出现了 " << cnt << " 次" << std::endl;
    }
    // std::cout << "输入的数值总和是" << sum << std::endl;
    return 0;
}